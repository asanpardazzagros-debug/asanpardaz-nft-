<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>داشبورد NFT آسان پرداز</title>
    <style>
        :root {
            --primary: #00f3ff;
            --secondary: #ff00ff;
            --accent: #00ff88;
            --bg-dark: #0a0a1f;
            --bg-light: #f0f0ff;
            --text-dark: #ffffff;
            --text-light: #1a1a3a;
            --card-bg-dark: rgba(16, 16, 48, 0.7);
            --card-bg-light: rgba(255, 255, 255, 0.9);
            --shadow-neon: 0 0 10px var(--primary), 0 0 20px var(--primary), 0 0 30px var(--primary);
            --shadow-neon-secondary: 0 0 10px var(--secondary), 0 0 20px var(--secondary), 0 0 30px var(--secondary);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: var(--bg-dark);
            color: var(--text-dark);
            transition: all 0.5s ease;
            min-height: 100vh;
            overflow-x: hidden;
